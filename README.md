# expr_simplify_by_degree_main
Based on a given set of variables, calculate the equivalent degree of each term in a polynomial or rational polynomial, then sort and select the top or bottom k degree terms for simplification.
